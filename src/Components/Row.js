const Row = ({ item } => {
    return (
        <tr>
            {Object.entries(item).map(([key, value]) => {
                return (
                    <Cell key=
                )
            }
        </tr>
    )
}

export default Row
